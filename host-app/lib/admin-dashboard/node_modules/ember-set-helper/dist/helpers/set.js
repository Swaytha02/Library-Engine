import { set as set$1 } from '@ember/object';
import { assert } from '@ember/debug';

function set(target, path, maybeValue) {
  assert('you must pass a path to {{set}}. You can pass a path statically, as in `{{set this "foo"}}`, or with the path dynamically, as in `{{set this this.greetingPath "Hello"}}`', typeof path === 'string' && path.length > 0 || typeof path === 'symbol' || typeof path === 'number');
  return arguments.length === 3 ? () => set$1(target, path, maybeValue) : value => set$1(target, path, value);
}

export { set as default };
//# sourceMappingURL=set.js.map
